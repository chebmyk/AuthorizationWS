package com.mika.ws;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

/*@RunWith(SpringRunner.class)
@SpringBootTest*/
public class WsApplicationTests {

	//@Test
	public void contextLoads() {
/*		Integer i = 10;
		Number n = 1.0;

		List<? extends  Number> inums = Arrays.asList(1,2,3);
		List<? super  Number> supInt = Arrays.asList(1,2,3);

		List<Number> nums = Arrays.asList(4,5,6);
		List<Integer> integers = Arrays.asList(7,8,9);

		nums.add(n);
		nums.add(i);
		inums.add(i);
		inums.add(n);
		integers.add(n);
		integers.add(i);
		supInt.add(n);
		supInt.add(i);

		n = inums.get(0);
		i = inums.get(0);

		i = supInt.get(0);
		n = supInt.get(0);

		n = nums.get(0);
		i = integers.get(0);


		nums = inums;
		inums = nums;
		integers = inums;
		inums = integers;

		nums = integers;
		integers = nums;




		System.out.println(inums);*/


	}

}
